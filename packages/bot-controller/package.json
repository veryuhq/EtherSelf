{
  "name": "etherself-bot",
  "version": "2026.4.1",
  "description": "Bot contrôleur pour EtherSelf, avec Components V2 (TypeScript)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "predeploy": "npm run build",
    "deploy": "node dist/deploy.js"
  },
  "dependencies": {
    "discord.js": "^14.25.1",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
