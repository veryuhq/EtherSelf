# Token du compte selfbot
TOKEN=

# Secret partagé avec le bot-controller (doit être identique des deux côtés, génère un secret avec un simple gestionnaire de mot de passe)
BRIDGE_SECRET=

# Port du serveur HTTP bridge
BRIDGE_PORT=3000