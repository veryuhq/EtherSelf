# Selfbot EtherSelf — discord.py-self
# ⚠️ discord.py-self s'importe sous le nom `discord` (incompatible avec discord.py officiel :
#    ne pas installer les deux dans le même environnement).
#
# L'extra [voice] est REQUIS pour le vocal : Discord impose désormais le protocole
# DAVE (E2EE) et ferme sinon la connexion avec le code 4017. L'extra tire PyNaCl
# (<1.6) ET davey (implémentation DAVE). Voir dolfies/discord.py-self#917.
discord.py-self[voice]>=2.1.0
aiohttp>=3.9
python-dotenv>=1.0
psutil>=5.9
