# Selfbot EtherSelf — discord.py-self
# ⚠️ discord.py-self s'importe sous le nom `discord` (incompatible avec discord.py officiel :
#    ne pas installer les deux dans le même environnement).
#
# 🔒 Version ÉPINGLÉE volontairement. Cette bibliothèque non officielle a le
# contrôle total du compte (token, gateway, requêtes REST) : c'est la plus
# grosse surface d'attaque de chaîne d'approvisionnement du projet. Monter la
# version reste un geste explicite, après avoir regardé le diff en amont.
discord.py-self==2.1.0

# Plancher haut volontaire : aiohttp est à la fois le serveur du bridge et le
# client HTTP, et les versions antérieures cumulent request smuggling, fuite
# d'en-têtes sur redirection cross-origin et DoS sur corps de requête.
aiohttp>=3.14.3

# Plancher pour GHSA-mf9w-mj56-hr94 (suivi de lien symbolique dans `set_key`).
python-dotenv>=1.2.2
psutil>=7.0
